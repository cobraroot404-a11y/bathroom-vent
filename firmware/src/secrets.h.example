// secrets.h.example -- copy this file to secrets.h and fill in real values.
// secrets.h is gitignored; never commit real credentials.

#pragma once

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASSWORD   "your-wifi-password"

#define MQTT_HOST       "192.168.1.10"
#define MQTT_PORT       1883
#define MQTT_USER       ""     // leave empty if the broker allows anonymous auth
#define MQTT_PASSWORD   ""

#define DEVICE_ID       "bathroom-vent-01"
